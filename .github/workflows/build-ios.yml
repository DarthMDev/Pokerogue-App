name: Build iOS IPA (Unsigned)

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: macos-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install project dependencies
        run: npm install

      - name: Install Capacitor packages
        run: |
          npm install \
            @capacitor/core \
            @capacitor/cli \
            @capacitor/ios \
            @capacitor/status-bar \
            @capacitor/splash-screen
          # Confirm cap binary is resolvable
          ./node_modules/.bin/cap --version

      - name: Add iOS platform (allow pod install failure)
        run: |
          # cap add ios will fail at pod install due to deployment target mismatch
          # but the Xcode project files we need ARE generated before it errors
          ./node_modules/.bin/cap add ios || true

          echo "=== Verifying project files exist ==="
          ls ios/App/
          echo ""
          echo "=== Podfile contents ==="
          cat ios/App/Podfile

      - name: Patch iOS deployment target to 16.0
        run: |
          echo "=== Patching Podfile ==="
          python3 - <<'EOF'
          import re

          with open('ios/App/Podfile', 'r') as f:
              podfile = f.read()

          # Replace whatever version Capacitor generated with 16.0
          if re.search(r"platform :ios,", podfile):
              podfile = re.sub(
                  r"platform :ios, ['\"][0-9.]+['\"]",
                  "platform :ios, '16.0'",
                  podfile
              )
          else:
              # Insert at top if line is missing entirely
              podfile = "platform :ios, '16.0'\n" + podfile

          with open('ios/App/Podfile', 'w') as f:
              f.write(podfile)

          print("Podfile patched successfully:")
          print(podfile[:400])
          EOF

          echo "=== Patching project.pbxproj ==="
          python3 - <<'EOF'
          import re

          path = 'ios/App/App.xcodeproj/project.pbxproj'
          with open(path, 'r') as f:
              content = f.read()

          count_before = content.count('IPHONEOS_DEPLOYMENT_TARGET')
          patched = re.sub(
              r'IPHONEOS_DEPLOYMENT_TARGET = [0-9.]+;',
              'IPHONEOS_DEPLOYMENT_TARGET = 16.0;',
              content
          )
          count_after = patched.count('IPHONEOS_DEPLOYMENT_TARGET = 16.0;')

          with open(path, 'w') as f:
              f.write(patched)

          print(f"pbxproj patched: {count_before} entries found, {count_after} set to 16.0")
          EOF

      - name: Run pod install with corrected target
        run: |
          cd ios/App
          pod install

      - name: Sync Capacitor (skip pod install, already done)
        run: ./node_modules/.bin/cap sync ios

      - name: Patch Info.plist
        run: |
          python3 - <<'EOF'
          import plistlib

          plist_path = 'ios/App/App/Info.plist'

          with open(plist_path, 'rb') as f:
              plist = plistlib.load(f)

          # ATS — allow pokerogue.net and Discord OAuth domains
          plist['NSAppTransportSecurity'] = {
              'NSAllowsArbitraryLoads': False,
              'NSExceptionDomains': {
                  'pokerogue.net': {
                      'NSIncludesSubdomains': True,
                      'NSExceptionAllowsInsecureHTTPLoads': False,
                      'NSExceptionRequiresForwardSecrecy': False,
                      'NSExceptionMinimumTLSVersion': 'TLSv1.2',
                  },
                  'discord.com': {
                      'NSIncludesSubdomains': True,
                      'NSExceptionAllowsInsecureHTTPLoads': False,
                  },
                  'discordapp.com': {
                      'NSIncludesSubdomains': True,
                      'NSExceptionAllowsInsecureHTTPLoads': False,
                  },
              }
          }

          # WKAppBoundDomains — required for cookie persistence on iOS 17+
          plist['WKAppBoundDomains'] = ['pokerogue.net']

          # Background modes — keeps process alive on lock/app switch
          plist['UIBackgroundModes'] = ['fetch']

          # Supported orientations — portrait + both landscapes
          plist['UISupportedInterfaceOrientations'] = [
              'UIInterfaceOrientationPortrait',
              'UIInterfaceOrientationLandscapeLeft',
              'UIInterfaceOrientationLandscapeRight',
          ]
          plist['UISupportedInterfaceOrientations~ipad'] = [
              'UIInterfaceOrientationPortrait',
              'UIInterfaceOrientationPortraitUpsideDown',
              'UIInterfaceOrientationLandscapeLeft',
              'UIInterfaceOrientationLandscapeRight',
          ]

          # Status bar
          plist['UIStatusBarStyle'] = 'UIStatusBarStyleLightContent'
          plist['UIViewControllerBasedStatusBarAppearance'] = True

          # Capacitor URL scheme — must match capacitor.config.ts
          existing = plist.get('CFBundleURLTypes', [])
          existing.append({'CFBundleURLSchemes': ['pokerogue']})
          plist['CFBundleURLTypes'] = existing

          with open(plist_path, 'wb') as f:
              plistlib.dump(plist, f)

          print("Info.plist patched successfully")
          EOF

      - name: Build unsigned archive
        run: |
          xcodebuild \
            -workspace ios/App/App.xcworkspace \
            -scheme App \
            -configuration Release \
            -archivePath $RUNNER_TEMP/App.xcarchive \
            archive \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            | xcpretty || true

          # Confirm archive was created
          echo "=== Archive contents ==="
          ls "$RUNNER_TEMP/App.xcarchive/Products/Applications/" 2>/dev/null \
            || (echo "Archive not found — xcodebuild may have failed" && exit 1)

      - name: Package into unsigned IPA
        run: |
          mkdir -p "$RUNNER_TEMP/Payload"
          cp -r \
            "$RUNNER_TEMP/App.xcarchive/Products/Applications/App.app" \
            "$RUNNER_TEMP/Payload/"
          cd "$RUNNER_TEMP"
          zip -r PokeRogue-unsigned.ipa Payload/
          echo "=== IPA size ==="
          du -sh PokeRogue-unsigned.ipa

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: PokeRogue-iOS-unsigned
          path: ${{ runner.temp }}/PokeRogue-unsigned.ipa
          retention-days: 30